# Clase BasePage

La clase `BasePage` actúa como el esqueleto fundamental de cualquier interacción. Contiene la delegación del `WebDriverWait` nativo para estandarizar de forma segura los clics y la inyección de texto sin tener que preocuparse de las excepciones.

::: pages.base_page
